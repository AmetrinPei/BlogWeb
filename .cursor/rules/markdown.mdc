---
description: Markdown 文档格式规范，避免 markdownlint 规格错误
globs: **/*.md
alwaysApply: false
---

# Markdown 格式规范

生成或编辑 `.md` 文件时，遵循以下格式以避免 markdownlint 告警：

## 表格

- 分隔行使用带空格的管道符：`| --- | --- |`，不要用 `|---|---|`
- 单元格内容与 `|` 之间保留一个空格
- 避免单元格内多余空格（如 `|  文字 |`）

## 代码块

- 围栏代码块必须标注语言标识
- 纯文本/流程图/目录树使用 `text`
- Markdown 模板示例使用 `markdown`

## 示例

```markdown
| 列 A | 列 B |
| --- | --- |
| 值 1 | 值 2 |

```text
纯文本流程说明
```
```
