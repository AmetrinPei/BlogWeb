# Place TLS files here (not committed):
#   fullchain.pem
#   privkey.pem
#
# Local self-signed (localhost):
#   PowerShell:  ..\scripts\gen-dev-certs.ps1
#   Bash:        ../scripts/gen-dev-certs.sh
# Production: copy Let's Encrypt or cloud certs to the same filenames.
